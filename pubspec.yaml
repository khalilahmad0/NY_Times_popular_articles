name: ny_times_popular_articles
description: NY Times mobile app that gets the most popular articles.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  http: ^0.13.4

dev_dependencies:
  flutter_test:
    sdk: flutter
  mockito: ^5.0.17
  build_runner: ^2.1.0
  network_image_mock: ^2.0.1
  flutter_lints: ^1.0.4

flutter:
  uses-material-design: true
